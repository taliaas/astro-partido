---
import RegisterContainer from "@/components/access/Register.vue";
import AuthLayout from "@/layouts/AuthLayout.astro";

const acceptTerms = Astro.url.searchParams.get("acceptTerms");
---

<AuthLayout>
  <RegisterContainer acceptTerms={acceptTerms === "true"} client:only="vue" />
</AuthLayout>
